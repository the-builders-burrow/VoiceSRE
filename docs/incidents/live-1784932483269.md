## VoiceSRE Autonomous Fix
**Incident:** TypeError: Cannot read properties of undefined (reading 'id') in getUser() (GITHUB_ACTIONS, production)
**Root cause:** The getUser function attempts to access the 'id' property on an undefined user object, likely because the database query returned null/undefined (user not found) and no null-check was performed before accessing properties.
**Explanation:** Added a guard clause to check if the user exists before accessing its properties. If the user is not found, an explicit error is thrown, preventing the TypeError and providing a clear failure mode.

```diff
--- a/app/lib/user.ts
+++ b/app/lib/user.ts
@@ -9,7 +9,10 @@
 export async function getUser(userId: string) {
   const user = await db.user.findUnique({ where: { id: userId } });
-  return { id: user.id, name: user.name };
+  if (!user) {
+    throw new Error('User not found');
+  }
+  return { id: user.id, name: user.name };
 }

```