## VoiceSRE Autonomous Fix
**Incident:** TypeError: Cannot read properties of undefined (reading 'id') in getUser() (GITHUB_ACTIONS, production)
**Root cause:** The getUser() function in app/lib/user.ts attempts to access the 'id' property of a user object that may be undefined, likely when no authenticated user is found. This causes a TypeError in production when the user session is missing or invalid.
**Explanation:** Added a guard clause to check if the user object is undefined before accessing its 'id' property. This prevents the TypeError and provides a clear error message when no user is found, allowing upstream callers to handle the missing authentication gracefully.

```diff
--- a/app/lib/user.ts
+++ b/app/lib/user.ts
@@ -9,6 +9,9 @@ export async function getUser() {
   // ... existing code to fetch user
   const user = await fetchUserFromSession();
+  if (!user) {
+    throw new Error('User not authenticated');
+  }
   return user.id;
 }
```